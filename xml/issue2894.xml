<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="2894" status="New">
<title>The function template <tt>std::apply()</tt> is required to be constexpr, but <tt>std::invoke()</tt> isn't. </title>
<section><sref ref="[func.invoke]"/></section>
<submitter>Great Britain</submitter>
<date>3 Feb 2017</date>
<priority>99</priority>

<discussion>
<b>Addresses GB 51</b>

<p>The function template <tt>std::apply()</tt> in <sref ref="[tuple.apply]"/> is required to be constexpr, but <tt>std::invoke()</tt> in <sref ref="[func.invoke]"/> isn't. The most sensible implementation of <tt>apply_impl()</tt> is exactly equivalent to <tt>std::invoke()</tt>, so this requires implementations to have a constexpr version of invoke() for internal use, and the public API std::invoke, which must not be constexpr even though it is probably implemented in terms of the internal version.</p>
<p>Proposed change: Add 'constexpr' to <tt>std::invoke</tt>.</p>

</discussion>

<resolution>
<p>
</p>
</resolution>

</issue>
