<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4532" status="New">
<title>Exposition-only function <tt><i>allocator-aware-forward</i></tt> uses `make_obj_using_allocator` incorrectly</title>
<section><sref ref="[exec.snd.expos]"/></section>
<submitter>Fraser Gordon</submitter>
<date>01 Mar 2026</date>
<priority>99</priority>

<discussion>
<p>
In <sref ref="[exec.snd.expos]"/> p49-50, an exposition-only template function 
<tt><i>allocator-aware-forward</i></tt> is defined. This function checks the environment for an 
allocator and, if one exists, uses it to create copies of the input object using that allocator, 
otherwise it forwards the object.
<p/>
The work of creating an object in an allocator-aware fashion is delegated to 
`make_obj_using_allocator` but uses it incorrectly.
<p/>
The parameters of `make_obj_using_allocator` (see <sref ref="[allocator.uses.construction]"/> p23) 
are <tt>(const Alloc&amp; alloc, Args&amp;&amp;... args)</tt> but the wording for 
<tt><i>allocator-aware-forward</i></tt> passes the arguments as `(obj, alloc)`. This will attempt 
to use the object as an allocator. Since the intent of <tt><i>allocator-aware-forward</i></tt> is 
to create a copy of an object using an allocator from the environment, this reversing does not 
appear to be intentional.
</p>
</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5032"/>.
</p>

<ol>

<li><p>Modify <sref ref="[exec.snd.expos]"/> as indicated:</p>

<blockquote>
<pre>
template&lt;class T, class Context&gt;
  decltype(auto) <i>allocator-aware-forward</i>(T&amp;&amp; obj, Context&amp;&amp; context); // <i>exposition only</i>
</pre>
<blockquote>
<p>
-49- <tt><i>allocator-aware-forward</i></tt> is an exposition-only function template used to either 
create a new object of type <tt>remove_cvref_t&lt;T&gt;</tt> from `obj` or forward `obj` depending on 
whether an allocator is available. If the environment associated with `context` provides an allocator 
(i.e., the expression `get_allocator(get_env(context))` is valid), let <tt><i>alloc</i></tt> be the result 
of this expression and let `P` be <tt>remove_cvref_t&lt;T&gt;</tt>.
<p/>
-50- <i>Returns</i>:
</p>
<ul style="list-style-type: none">
<li><p>
(50.1) &mdash; If <tt><i>alloc</i></tt> is not defined, returns <tt>std::forward&lt;T&gt;(obj)</tt>,
</p></li>
<li><p>
(50.2) &mdash; otherwise if `P` is a specialization of <tt><i>product-type</i></tt>, returns an 
object of type `P` whose elements are initialized using
</p>
<blockquote><pre>
make_obj_using_allocator&lt;decltype(e)&gt;(<ins><i>alloc</i>,</ins> std::forward_like&lt;T&gt;(e)<del>, <i>alloc</i></del>)
</pre></blockquote>
<p>
where `e` is the corresponding element of `obj`,
</p>
</li>
<li><p>
(50.3) &mdash; otherwise, returns <tt>make_obj_using_allocator&lt;P&gt;(<ins><i>alloc</i>,</ins> std::forward&lt;T&gt;(obj)<del>, <i>alloc</i></del>)</tt>.
</p></li>
</ul>
</blockquote>
</blockquote>
</li>

</ol>
</resolution>

</issue>
