<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4546" status="New">
<title>Integer-class type should be structural type</title>
<section>
<sref ref="[iterator.concept.winc]"/>
</section>
<submitter>Hewill Kang</submitter>
<date>19 Mar 2026</date>
<priority>99</priority>

<discussion>
<p>
Currently, integer-class types are not guaranteed to be structure types, so the range of <code>size</code> members
that return integer-class types may not be able to model <code><i>tiny-range</i></code>, because its size value 
cannot be used as a template parameter even if it is a constant expression. This does not appear to be the intention.
<p/>
Given that integer-class types should behave like regular integers, and those types in MSVC-STL and libstdc++ are 
both implemented as structural types, it would be better to be clear about the wording.
</p>
</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5032"/>.
</p>

<ol>

<li><p>Modify <sref ref="[iterator.concept.winc]"/> as indicated:</p>

<blockquote>
<p>
-9- All integer-class types <ins>are structural types (<sref ref="[temp.param]"/>) that</ins> model 
`regular` (<sref ref="[concepts.object]"/>) and <tt>three_way_comparable&lt;strong_ordering&gt;</tt> 
(<sref ref="[cmp.concept]"/>).
</p>
</blockquote>
</li>

</ol>
</resolution>

</issue>
