<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4497" status="Immediate">
<title>`std::nullopt_t` should be comparable</title>
<section><sref ref="[optional.nullopt]"/></section>
<submitter>Barry Revzin</submitter>
<date>21 Dec 2025</date>
<priority>2</priority>

<discussion>
<p>
`std::nullopt_t` currently has no comparison operators. This prevents perfectly reasonable code from working, 
like `ranges::find(v, nullopt)` where `v` is a <tt>vector&lt;optional&lt;T&gt;&gt;</tt>, for no good reason. 
<p/>
Additionally, <tt>optional&lt;T&gt;</tt> has the full set of comparisons. But <tt>optional&lt;T&gt;</tt> is 
conceptually a <tt>variant&lt;nullopt_t, T&gt;</tt>, which wouldn't be comparable... because of `nullopt_t`. 
Other empty types like <tt>tuple&lt;&gt;</tt> and `monostate` are also comparable.
<p/>
Proposed resolution: Add a defaulted member <tt>operator&lt;=&gt;</tt> to `nullopt_t`. 
</p>

<note>2026-02-18; Reflector poll.</note>
<p>
Set priority to 2 after reflector poll.
</p>
<p>
Discussion whether this needs LEWG approval.
Paper <paper num="P2405"/>, for which the <code>nullopt</code>
part received LEWG support, is relevant here.
</p>

<note>2026-03-26; Tim provides wording</note>

<note>Croydon 2026-03-27; Status changed: New &rarr; Immediate.</note>
</discussion>

<resolution>

<p>
This wording is relative to <paper num="N5032"/>.
</p>

<ol>
<li>
<p>Edit <sref ref="[optional.nullopt]"/>, as indicated:</p>

<blockquote>
<pre>
struct nullopt_t{<i>see below</i>};
inline constexpr nullopt_t nullopt(unspecified);
</pre>
</blockquote>
<p>
-1- [&hellip;]
<p/>
-2- Type `nullopt_t` <del>shall</del><ins>does</ins> not have a default constructor 
or an initializer-list constructor, and <del>shall not be</del><ins>is not</ins> an aggregate.
<ins>`nullopt_t` models `copyable` and <code>three_way_comparable&lt;strong_ordering&gt;</code>.</ins>
</p>
</li>
</ol>
</resolution>

</issue>
