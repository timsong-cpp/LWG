<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4563" status="New">
<title>Missing "round to nearest, ties away from zero" mode</title>
<section>
<sref ref="[round.style]"/>
</section>
<submitter>Jan Schultke</submitter>
<date>30 Mar 2026</date>
<priority>99</priority>

<discussion>
<p>
As discussed in LWG <iref ref="4474"/> (approved for C++26), `std::round_style` is intended to be 
symmetrical with `FLT_ROUNDS`. LWG <iref ref="4474"/> clarified that `round_to_nearest` corresponds to 
an `FLT_ROUNDS` value of 1, meaning "to nearest, ties to even".
<p/>
Unfortunately, any implementation using the mode "to nearest, ties away from zero" (corresponding 
to an `FLT_ROUNDS` value of 4 and supported by ISO/IEC 60559 floating-point implementations) is forced 
to stop using `round_to_nearest` as a <tt>numeric_limits&lt;float&gt;::round_style</tt> constant due 
to stricter requirements for that enumerator. To prevent such implementations from resorting to a 
non-standard enumerator, a new enumerator is needed.
<p/>
The name should be inspired by C23 macros such as `FE_TONEARESTFROMZERO`.
<p/>
See also <a href="https://cigix.me/c23#5.2.5.3.3.p23">ISO/IEC 9899:2024 §5.2.5.3 paragraph 23</a>
</p>
</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5032"/> after application of LWG motion 1
(See <a href="https://wiki.isocpp.org/uploads/2026-03_Croydon-LWGStrawPollsStaging_-_P4145r0.html#4474">LWG Motion 1-#4474</a>).
</p>

<ol>
<li>
<p>Modify <sref ref="[round.style]"/> as indicated:</p>

<blockquote>
<blockquote>
<pre>
namespace std {
  enum float_round_style {
    round_indeterminate = -1,
    round_toward_zero = 0,
    round_to_nearest = 1,
    round_toward_infinity = 2,
    round_toward_neg_infinity = 3<ins>,
    round_to_nearest_from_zero = 4</ins>
  };
}
</pre>
</blockquote>
<blockquote>
<p>
-1- The rounding mode for floating-point arithmetic is characterized by the values:
</p>
<ul style="list-style-type: none">
<li><p>
(1.1) &mdash; `round_indeterminate` if the rounding style is indeterminable
</p></li>
<li><p>
(1.2) &mdash; `round_toward_zero` if the rounding style is toward zero
</p></li>
<li><p>
(1.3) &mdash; `round_to_nearest` if the rounding style is to the nearest representable value; if there 
are two equally near such values, the one with an even least significant digit is chosen
</p></li>
<li><p>
(1.4) &mdash; `round_toward_infinity` if the rounding style is toward infinity
</p></li>
<li><p>
(1.5) &mdash; `round_toward_neg_infinity` if the rounding style is toward negative infinity
</p></li>
<li><p>
<ins>(1.?) &mdash; `round_to_nearest_from_zero` if the rounding style is to the nearest 
representable value; if there are two equally near such values, the one with greater magnitude is chosen</ins>
</p></li>
</ul>
</blockquote>
</blockquote>
</li>

</ol>
</resolution>

</issue>
