<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4478" status="Immediate">
<title>`meta::has_identifier` is not specified for annotations</title>
<section><sref ref="[meta.reflection.names]"/></section>
<submitter>Jakub Jelinek</submitter>
<date>18 Nov 2025</date>
<priority>1</priority>

<discussion>
<p>
The `std::meta::has_identifier` metafunction doesn't say what it returns
for an annotation. An annotation is not an entity, so it's not an unnamed
entity, and it's also not a type, type alias, function, or any of the other
things in the list.
</p>
<p>
We should add an item to the list for annotations,
or just add a catch-all "Otherwise false" to the end of the list.
</p>

<note>2025-11-26; Reflector poll.</note>
<p>
Set priority to 1 after reflector poll.
</p>
<p>
'value' and 'object' are missing too.
</p>

<note>Croydon 2026-03-25; LWG adds wording</note>


<note>Croydon 2026-03-25; move to Immediate.</note>
</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5032"/>.
</p>

<ol>
<li>
<p>Modify <sref ref="[meta.reflection.names]"/> as indicated:</p>
<blockquote><pre>consteval bool has_identifier(info r);</pre>
<blockquote>-1- <i>Returns</i>:
<ol style="list-style-type: none"><li>&mdash; [...]</li>
<li>&mdash; Otherwise, <ins>if</ins> `r` represents a data member description
(T,N,A,W,NUA) (<sref ref="[class.mem.general]"/>); `true` if N is not ⊥.
Otherwise, `false`.</li>
<li><ins>&mdash; Otherwise, `false`.</ins></li>
</ol>
</blockquote>
</blockquote>
</li>
</ol>

</resolution>

</issue>
