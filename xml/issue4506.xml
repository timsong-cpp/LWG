<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4506" status="New">
<title>`source_location` is explicitly unspecified if is constexpr or not</title>
<section><sref ref="[support.srcloc.class]"/></section>
<submitter>Hana Dusíková</submitter>
<date>27 Dec 2025</date>
<priority>2</priority>

<discussion>
<p>
Basically in <sref ref="[support.srcloc.class.general]"/> following p1 there is a note from Jens 
from six years ago which says (emphasis mine):
</p>
<blockquote style="border-left: 3px solid #ccc;padding-left: 15px;">
<p>
[<i>Note 1</i>: The intent of `source_location` is to have a small size and efficient copying. 
It is unspecified whether the copy/move constructors and the copy/move assignment operators 
are trivial <b>and/or constexpr</b>. &mdash; <i>end note</i>]
</p>
</blockquote>
<p>
But also reflection's `std::meta::source_location_of` returns it by value and is `consteval`. 
This means `source_location` needs to be specified to constexpr. And good news is ... all three 
major implementations have it constexpr implicitly.
<p/>
Options are (from my perspective) to remove that part of the note "and/or constexpr" and 
just call it a day, or add
</p>
<blockquote><pre>
constexpr source_location(const source_location&amp;) = default;
constexpr source_location(source_location&amp;&amp;) noexcept = default;
constexpr source_location&amp; operator=(const source_location&amp;) = default;
constexpr source_location&amp; operator=(source_location&amp;&amp;) noexcept = default;
</pre></blockquote>
<p>
AFAIK this is how to explicitly say it must be constexpr but it can still be trivial, see
<a href="https://compiler-explorer.com/z/5v5ndd7z1">demo</a>.
</p>

<note>2026-02-18; Reflector poll.</note>
<p>
Set priority to 2 after reflector poll.
</p>

<note>2026-03-26; Tim adds wording</note>

</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5032"/>.
</p>

<blockquote class="note">
<p>
[<i>Drafting note:</i> <sref ref="[functions.within.classes]"/> can be read to say that the
functions are constexpr already, but given the note that contradicts this interpretion,
explicit declarations can't hurt.
&mdash; <i>end drafting note</i>]
</p>
</blockquote>

<ol>
<li><p>Modify <sref ref="[support.srcloc.class.general]"/> as indicated:</p>

<blockquote>
<pre>
namespace std {
  struct source_location {
    // source location construction
    static consteval source_location current() noexcept;
    constexpr source_location() noexcept;

    <ins>constexpr source_location(const source_location&amp;) noexcept = default;</ins>
    <ins>constexpr source_location&amp; operator=(const source_location&amp;) noexcept = default;</ins>

    // source location field access
    constexpr uint_least32_t line() const noexcept;
    constexpr uint_least32_t column() const noexcept;
    constexpr const char* file_name() const noexcept;
    constexpr const char* function_name() const noexcept;

  private:
    uint_least32_t line_;               // exposition only
    uint_least32_t column_;             // exposition only
    const char* file_name_;             // exposition only
    const char* function_name_;         // exposition only
  };
}
</pre>
<p>
- 1- The type `source_location` <del>meets the <i>Cpp17DefaultConstructible</i>,
<i>Cpp17CopyConstructible</i>, <i>Cpp17CopyAssignable</i>, <i>Cpp17Swappable</i>,
and <i>Cpp17Destructible</i> requirements 
(<sref ref="[utility.arg.requirements]"/>, <sref ref="[swappable.requirements]"/>)</del>
<ins>models `semiregular`</ins>. <ins><code>is_nothrow_swappable_v&lt;source_location&gt;</code> is `true`.</ins>
<del>All of the following conditions are `true`:</del>
</p>
<ol style="list-style-type: none">
<li><p><del>(1.1) &mdash; <code>is_nothrow_move_constructible_v&lt;source_location&gt;</code></del></p></li>
<li><p><del>(1.2) &mdash; <code>is_nothrow_move_assignable_v&lt;source_location&gt;</code></del></p></li>
<li><p><del>(1.3) &mdash; <code>is_nothrow_swappable_v&lt;source_location&gt;</code></del></p></li>
</ol>
<p>
[<i>Note</i> 1: The intent of `source_location` is to have a small size and efficient copying. 
It is unspecified whether the copy/move constructors and the copy/move assignment operators are trivial <del>and/or constexpr</del>. 
&mdash; <i>end note</i>]
</p>
</blockquote>
</li>
</ol>
</resolution>

</issue>
